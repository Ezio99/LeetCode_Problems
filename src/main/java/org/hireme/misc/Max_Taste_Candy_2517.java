package org.hireme.misc;

import java.util.Arrays;

public class Max_Taste_Candy_2517 {
    public int maximumTastiness(int[] price, int k) {
        Arrays.sort(price);
        int l = 0, r = price.length - 1;
        int ctr = 0, diff = 0;
        while (l != r) {

        }

        System.out.println(Arrays.toString(price));

        return diff;
    }
}
